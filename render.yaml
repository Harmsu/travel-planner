services:
  - type: web
    name: travel-planner
    runtime: node
    buildCommand: npm run build && cd server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: APP_PASSWORD
        sync: false
      - key: NODE_ENV
        value: production
